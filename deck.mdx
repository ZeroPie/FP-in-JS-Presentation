import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";

export const theme = vsDark;

# FP 👋

---

<CodeSurfer>

```js

Was ist Functional Programming?

```

```markdown title='Functional Programming'
The process of building software by composing pure functions.
avoiding shared state, mutable data, and side-effects 
```

```markdown title='Side Effects'
  A side effect is any application state change that is 
  observable outside the called function other than its return value. 
```

```markdown title='Pure Function'
  Given the same input, will always return the same output.
  Produces no side effects.
```

```js file=./codesnipets/caseforPurity1.js title='Purity'
```

```js file=./codesnipets/caseForPurity2.js title=Purity 
```

```js title='Purity'
  
  var counter = 1

  incrementCounter()

  console.log(counter) // 2
  
```

```js 4 title='Purity' subtitle=' ️⚠️️ Impurity Indikator: Funktionen deren return Value nicht benutzt wird'
  
  var counter = 1

  incrementCounter()

  console.log(counter) // 2
  
```

```js title='Is it Pure?'

const buttonId = 'ms-button'

const findButton = () => 
  document.getElementById(buttonId)

```

```js title='Is it Pure?'

const findButton = buttonId => 
  document.getElementById(buttonId)

```

```js title='Is it Pure?'

const findButton = (buttonId, document) =>
  document.getElementById(buttonId)
  
```

```js file=./codesnipets/daysThisMonthImpure.js
```

```js file=./codesnipets/daysThisMonthPure.js
```

```js
Functional programming is declarative rather than imperative,
and application state flows through pure functions.
```

</CodeSurfer>

---

<CodeSurferColumns>

<Step>

```js file=./codesnipets/doubleitems.js title='Imperativ'
```

```js title='Declarativ'
const doubleItems = arr => arr.map(v => v * 2)
```

</Step>

<Step subtitle="Mutation">

```js 4[18:22] file=./codesnipets/doubleitems.js
```

```js
const doubleItems = arr => arr.map(v => v * 2)
```

</Step>

<Step subtitle="Impure Variante">

```js 1:7 file=./codesnipets/doubleItemsMutate.js
```

```js
const doubleItems = arr => arr.map(v => v * 2)
```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()
push()
shift()
unshift()
```

```js
slice() 🍕
concat()
filter()
map()
reduce()
```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()

const pizzaArr = [0,1,2,3]  // const ¯\_( ツ )_/¯
slices.splice(0,2)
pizzaArr // [🍕. 🍕, 🍕] 

pop()
push()
shift()
unshift()
```

```js
slice() 🍕

const arr1 = [🍕, 🍕, 🍕, 🍕, 🍕];
const arr2 = arr1.slice(0, 2) // [🍕. 🍕, 🍕]

concat()
filter()
map()
reduce()
```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()

const fruits = [🍏, 🍑, 🍒]
fruits.pop()

push()
shift()
unshift()
```

```js
slice() 🍕

const fruits = [🍏, 🍑, 🍒]
const poppedFruits = fruits.slice(0, -1)

concat()
filter()
map()
reduce()
```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()
push()

const arr = [1,2]
arr.push(3)

shift()
unshift()
```

```js
slice() 🍕
concat()

const arr = [1,2]
const arr2 = arr1.concat[3]

filter()
map()
reduce()
```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()
push()

const arr = [1,2]
arr.push(3)

shift()
unshift()
```

```js
slice() 🍕
concat()
...spread operator

const arr1 = [1,2]
const arr2 = [...arr1, 3]

filter()
map()
reduce()

```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()
push()
shift()

const arr = [1,2,3]
const firstEle = arr.shift()  // 1, [2,3]

unshift()
```

```js
slice() 🍕
const arr1 = [1,2,3]
const arr2 = arr.slice(1)

concat()
filter()
map()
reduce()

```

</Step>

<Step title="Array Methods">

```js title='Impure'
splice()
pop()
push()
shift()
unshift()

const arr = [1,2,3]
const arrLenght = arr.unshit(-1, 0) // 5 , [0,-1,1,2,3]

```

```js
slice() 🍕
concat()
filter()
map()
reduce()

...spread operator
const arr = [1,2,3]
const newArr = [ -1, 0,...arr]
const arrLenght = newArr.length

```

</Step>

<Step title="Currying 🍛">

```js

const addBy = x, y => x => x + y  

```js

"use strict";

var addBy = function sum(x, y) {
  return function (x) {
    return x + y;
  };
};

```js

var prepareFood = function prepareFood(a, b) {
  return function (b) {
    return a(b)
  }
}

```

</Step>


</CodeSurferColumns>

---

[FP Roadmap:](https://hub.packtpub.com/a-five-level-learning-roadmap-for-functional-programmers/)
